<div class="admin_segment">
  <h3>Users</h3>
  <div class="table_actions">
    <%= link_to 'Add New User', new_user_path, :class => "admin_btn  right" %>
  </div>
  <table id="dashboard_users_table" class="display">
    <thead>  
      <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <th>Created On</th>
        <th>Edit User</th>
        <th>Delete User</th>
      </tr>
    </thead>

    <tbody>
      <% users.each do |user| %>
			<% unless user.blank? %>
        <tr>
          <td><%=h (user.first_name).titleize %></td>
          <td><%=h (user.last_name).titleize %></td>
          <td><%=h user.email %></td>
					<td><%=h (user.created_at).to_s(:full) %></td>
          <td><%= link_to 'Edit User', edit_user_path(user), :class => "update" %></td>
          <td><%= button_to 'Delete User', user, :confirm => 'Are you sure?', :method => :delete, :class => "delete" %></td>
        </tr>
			<% end %>
      <% end %>
    </tbody>
  </table>
</div>